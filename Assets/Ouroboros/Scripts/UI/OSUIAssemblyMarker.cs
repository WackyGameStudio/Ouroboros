namespace Ouroboros.UI
{
    /// <summary>
    /// Marks the UI assembly boundary until Step 04 introduces concrete HUD views.
    /// </summary>
    public static class OSUIAssemblyMarker
    {
    }
}
